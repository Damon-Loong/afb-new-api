# New-API Docker Compose Configuration
# 
# Quick Start:
#   1. docker-compose up -d
#   2. API / dashboard (embedded web): http://localhost:3000
#   3. Mopc site (Mopc-web-site): http://localhost:3001  (static only; Mopc calls API via public URLs — see Mopc-web-site/src/api/*.ts defaults or pass VITE_* build args)
#
# Using MySQL instead of PostgreSQL:
#   1. Comment out the postgres service and SQL_DSN line 15
#   2. Uncomment the mysql service and SQL_DSN line 16
#   3. Uncomment mysql in depends_on (line 28)
#   4. Uncomment mysql_data in volumes section (line 64)
#
# ⚠️  IMPORTANT: Change all default passwords before deploying to production!

version: '3.4' # For compatibility with older Docker versions

services:
  new-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: afb-api:latest
    container_name: new-api
    restart: always
    command: --log-dir /app/logs
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./logs:/app/logs
    environment:
      - SQL_DSN=root:replace_with_mysql_password@tcp(replace_with_mysql_host:3306)/new-api?charset=utf8mb4&parseTime=True&loc=Local # ⚠️ IMPORTANT: Replace with your remote MySQL connection
      - REDIS_CONN_STRING=redis://:replace_with_redis_password@replace_with_redis_host:6379/0 # ⚠️ IMPORTANT: Replace with your remote Redis connection
      - TZ=Asia/Shanghai
      - ERROR_LOG_ENABLED=true # 是否启用错误日志记录 (Whether to enable error log recording)
      - BATCH_UPDATE_ENABLED=true  # 是否启用批量更新 (Whether to enable batch update)
      # 需求市场（即梦）活动同步：默认开启。启动 30 秒后执行首次同步，然后按间隔后台刷新
      - MARKET_JIMENG_SYNC_ENABLED=true
      - MARKET_JIMENG_SYNC_INTERVAL=6h
      - MARKET_JIMENG_SYNC_LIMIT=0
      - MARKET_JIMENG_SYNC_TIMEOUT=45s
      - NODE_NAME=new-api-node-1  # 节点名称，用于审计日志中标识节点身份；多节点/容器部署时建议设置 (Node name used in audit logs; recommended when running multiple instances or in containers)
      - SESSION_SECRET=replace_with_a_strong_random_session_secret # ⚠️ IMPORTANT: Must be changed in production
      - CRYPTO_SECRET=replace_with_a_strong_random_crypto_secret # ⚠️ IMPORTANT: Must be changed in production
#      - STREAMING_TIMEOUT=300  # 流模式无响应超时时间，单位秒，默认120秒，如果出现空补全可以尝试改为更大值 （Streaming timeout in seconds, default is 120s. Increase if experiencing empty completions）
#      - SYNC_FREQUENCY=60  # Uncomment if regular database syncing is needed
#      - GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX  # Google Analytics 的测量 ID (Google Analytics Measurement ID)
#      - UMAMI_WEBSITE_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx  # Umami 网站 ID (Umami Website ID)
#      - UMAMI_SCRIPT_URL=https://analytics.umami.is/script.js  # Umami 脚本 URL，默认为官方地址 (Umami Script URL, defaults to official URL)
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:3000/api/status | grep -o '\"success\":\\s*true' || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3

  mopc-web:
    build:
      context: ./Mopc-web-site
      dockerfile: Dockerfile
      # Optional: point Mopc at your public API (omit to use defaults baked in src/api/*.ts)
      # args:
      #   VITE_API_BASE_URL: https://your-domain.example.com/api
      #   VITE_V1_BASE_URL: https://your-domain.example.com/v1
    image: afb-mopc-web:latest
    container_name: mopc-web
    restart: always
    ports:
      - "3001:80"
